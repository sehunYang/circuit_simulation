# 에디터 / 도구 상태
.omc/
.vscode/
.idea/

# OS 생성 파일
.DS_Store
Thumbs.db
desktop.ini

# 로그 / 임시
*.log
*.tmp

# 로고 원본 (2380px, ~900KB) — 아이콘은 assets/logo.png(512px) 에서 재생성 가능하므로
# 저장소·배포에는 넣지 않는다. 필요하면 이 줄을 지우고 추적하면 된다.
양세훈 로고.png

# 브라우저 테스트용 (npm i --no-save puppeteer-core) — 저장소에 넣지 않는다
node_modules/
package.json
package-lock.json

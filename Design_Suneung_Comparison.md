# 수능 물리 문항 작도 규격 적용 (전기회로)

형제 프로젝트 [mechanics_simulation](https://github.com/sehunYang/mechanics_simulation) 이
2022~2026학년도 수능 **물리학Ⅰ·Ⅱ** 문항지(한국교육과정평가원 공개 기출) 5개년의 작도 관례를
정리해 적용한 규격([Design_Suneung_Comparison.md](https://github.com/sehunYang/mechanics_simulation/blob/main/Design_Suneung_Comparison.md))을
이 프로젝트에도 맞췄다.

> 참고 자료는 관례 파악에만 사용했고, 원본 그림을 복제하거나 저장소·배포본에 포함하지 않았다.
> 저장소에 들어간 것은 전부 이 규격에 따라 새로 작도하거나 기존 작도를 재색(再色)한 것이다.
> (작도 관례 자체는 저작권 보호 대상이 아니다 — 보호되는 것은 개별 그림의 표현이다.)

## 1. 역학 시뮬레이터와 다른 점 — 소자 디자인 불변

역학 쪽은 물체·용수철·도르래의 **모양 자체를** 수능 규격으로 다시 작도했다.
이 프로젝트는 다르다. **회로 소자 심볼은 이미 완성된 디자인이므로 기하를 건드리지 않는다.**

| 항목 | 역학 | 회로 (이 프로젝트) |
|---|---|---|
| 요소 모양 | 새로 작도 (지그재그 → 나선 코일 등) | **변경 없음** — 기존 심볼 그대로 |
| 색 | 다크 + 컬러 → 흑백 | 다크 + 컬러 → **흑백** |
| 서체 | 모노스페이스 → HyhwpEQ | 모노스페이스 → **HyhwpEQ** |
| 벡터 내보내기 | SVG path 를 새로 정의 | **기존 그리기 함수를 재사용** (§3) |

`src/js/render/symbols.js` 의 `drawDCSource` · `drawACSource` · `drawResistor` ·
`drawCapacitor` · `drawInductor` · `drawJunction` 은 **한 줄도 바뀌지 않았다.**
바뀐 것은 그 함수들을 호출할 때의 색·선굵기·서체뿐이다.

## 2. 대조표

| 요소 | 수능 규격 | 변경 전 | 변경 후 |
|---|---|---|---|
| 전체 톤 | 흰 바탕, 검정 선 + 회색 채움만 | 다크 배경 + 컬러 | **흰 바탕 흑백** |
| 캔버스 무대 | 흰 지면 | `#0d0f1a` | **`#ffffff`** |
| 소자 심볼 | 가는 검정 실선 | `#c8e0f4` (하늘색) | **검정** (모양은 동일) |
| 도선 | 가는 검정 실선 한 줄 | `#4a6a90` 파랑 | **검정 실선 1.7px** |
| 특성값 라벨 | 글자만, 배경 상자 없음 | 진한 알약 박스 + 파란 테두리 + 글로우 | **검정 이탤릭 + 흰 헤일로** |
| 전류 화살표 | 가는 선 + 속 찬 삼각 화살촉 | 주황 삼각형 | **검정 삼각 + 흰 테두리** |
| 전류 배지 | 글자만 | 진한 박스 + 파란 글로우 | **검정 이탤릭 + 흰 헤일로** |
| 전자 애니메이션 | (수능엔 없음) | 하늘색 + 글로우 | **검정 원 + 흰 테두리, 잔상은 회색 농도** |
| 격자 배경 | (수능엔 없음) | 파란 점 | **아주 옅은 회색 점** |
| 과도 응답 그래프 | 흰 바탕, 검정 축 | 다크 + 형광색 곡선 | **흰 바탕 · 검정 축 · 인쇄용 어두운 곡선색** |
| 서체 | 수식 세리프 이탤릭 / 한글 명조 | `Courier New` | **HyhwpEQ + 맑은 고딕** (§4) |
| UI 크롬 | — | 다크 글래스 | **밝은 회색 + 파란 강조** |
| 촬영 결과물 | 벡터 선화 | PNG (화면 배율 종속) | **SVG** (§3) |

### 편집 보조 표시는 흑백에서 제외

선택 박스·포트 점·도선 미리보기·드래그 고스트는 **그림이 아니라 UI** 다.
이들만 파랑(`#2563eb`)·초록(`#128a53`)·빨강(`#c0392b`) 을 쓴다.
편집이 불가능해지는 것을 막기 위한 것이며, **촬영(SVG) 결과물에는 포함되지 않는다.**

## 3. 구현 방식 — Canvas2D → SVG 기록기

역학은 모양을 SVG path 문자열로 정의하고 `Path2D` 로 캔버스에 그렸다.
회로는 **반대 방향**으로 풀었다. 소자 디자인을 못 바꾸므로, 기존 Canvas 그리기 코드를
그대로 두고 **Canvas2D 경로 API 를 흉내내는 기록기**(`App.SN.Recorder`)를 만들었다.

```js
App.Symbols.draw(ctx,      type, cx, cy, size, rot)   // → 화면 (Canvas)
App.Symbols.draw(recorder, type, cx, cy, size, rot)   // → SVG  (<path d="…">)
```

`Recorder` 는 `save/restore` · `translate/rotate/scale` · `moveTo/lineTo/arc/`
`bezierCurveTo/quadraticCurveTo/closePath` · `stroke/fill` 을 구현하고, 2×3 변환 행렬을
좌표에 **미리 접어 넣어** path 를 만든다. 따라서

- 심볼 코드를 한 줄도 고치지 않고 벡터 내보내기를 얻는다
- **보이는 그림과 내보낸 벡터가 같은 함수를 통과**하므로 기하가 100% 일치한다

`arc()` 는 SVG 호가 180°를 넘으면 방향이 모호해지므로 π 단위로 쪼갠다 (한 바퀴 = 정확히 2조각).
회전 변환 아래에서도 원은 원으로 남는다(강체 변환이라 반지름 보존).

### 촬영 = SVG

`촬영` 버튼은 이제 PNG 가 아니라 흰 바탕·검정 선 **SVG 파일**을 저장한다.
학습지·시험지에 확대해 붙여도 깨지지 않는다.

좌표는 **뷰포트와 무관하게 `scale=1`, `offset=0`** 으로 고정해서 그린다
(그리드 1칸 = `CELL_SIZE`). 화면을 얼마나 확대해 두었든 같은 회로는 항상 같은 SVG 를 낸다.

## 4. 서체

수능 문항지 조판과 같은 계열로 맞췄다. 브라우저는 **글리프 단위로 폴백**하므로
한 스택에 둘을 넣으면 문자 종류에 따라 자동으로 갈린다.

| 대상 | 서체 |
|---|---|
| 영문·숫자·수식 | **HyhwpEQ** (한글 수식 서체, HWP 설치 시 함께 깔림) |
| 한글 | **맑은 고딕** |
| 폴백 | sans-serif |

> HyhwpEQ 는 **로컬 설치 서체**라 HWP 가 없는 기기에서는 맑은 고딕/sans-serif 로
> 폴백된다 — 형태는 유지되지만 자형이 다를 수 있다.

라벨 크기는 `App.SN.FS` 토큰으로 모아 두고 가독성 하한을 뒀다:
소자 특성값 10–14 px, 전류 배지 9–13 px, 안내 문구 12 px.

## 5. 적용하지 않은 관례와 이유

| 관례 | 미적용 사유 |
|---|---|
| **소자 모양 재작도** | 회로 소자 심볼은 이미 완성된 디자인이다. 규격 적용은 색·서체에 한정한다. (이 문서의 전제) |
| **치수선**(양끝 화살표 + 점선 보조선) | 시뮬레이터에 "표시할 치수"라는 개념이 없다. |
| **격자 제거** | 수능 그림에는 격자가 없지만, 배치·정렬을 돕는 편집 도구라 아주 옅게 남겼다. 촬영(SVG) 에는 포함되지 않는다. |
| **비유 모드 3D 흑백화** | 수로 비유는 문항 그림이 아니라 물리적 은유다. 물=파랑, 저항 물레방아=주황처럼 **색이 곧 의미**라 유지했다. 다만 바닥·안개·조명 등 **환경**은 지면 톤으로 밝게 맞춰 앱 전체가 한 벌로 읽히게 했다. |
| **과도 응답 그래프 완전 흑백** | 여러 소자의 곡선을 동시에 겹쳐 그리므로 구분이 필요하다. 흰 바탕·검정 축은 규격대로 두고, 곡선만 **인쇄해도 명도가 서로 다른** 어두운 색 6종을 쓴다. |

## 6. 검증

헤드리스(Node `vm` + 스텁 2D 컨텍스트, Chrome `--headless --screenshot`)로 **65항목** 검증.

| 항목 | 내용 |
|---|---|
| Recorder 좌표 정확도 | 저항·커패시터·DC·AC·인덕터의 단자/판/진폭 좌표가 심볼 정의값과 일치 (회전 0°·90° 모두) |
| `arc()` 변환 | 전원 원의 반지름·시작점·분할 수·sweep 플래그, 회전 후 반지름 보존, 역방향(ccw) 호 |
| SVG 유효성 | XML 선언·태그 균형·viewBox·흰 바탕 rect·NaN 없음·빈 `d` 없음 |
| **유채색 0개** | 내보낸 SVG 의 모든 `fill`/`stroke` 가 `none`/`#000000`/`#ffffff` |
| 편집 보조 표시 제외 | SVG 에 파랑·초록 UI 색이 하나도 없음 |
| 화면 좌표 = 내보내기 좌표 | `App.Geo.getPortPixel()`(scale=1) 과 SVG 경로 좌표 일치 |
| 렌더 무오류 | 선택·연결중·플래시·솔버 오버레이·에러 배너 전 분기 실행 |
| 캔버스 색·서체 | 검정 잉크 사용, 허용 밖 색 0개, `Courier New` 잔존 0개, HyhwpEQ 스택 적용, 흰 헤일로 동작 |
| 표시 토글 반영 | `showLabels=false` → SVG `<text>` 0개, 심볼은 유지 |
| 시각 대조 | 같은 소자 6종을 Canvas 와 SVG 로 나란히 렌더 → **형태 동일** 확인 |

전체 **65/65 통과**.
